#!/usr/bin/env python3
# encoding: utf-8

import argparse
from os.path import join, split, expanduser, realpath

from filesystem import link


def setup():
    print('Creating symlinks...')
    home = expanduser('~')
    # PuDB config directory
    path, fl = split(realpath(__file__))
    src = join(path, 'src', 'pudb')
    tgt = join(home, '.config', 'pudb')
    link(src, tgt, 'PuDB config directory')


if __name__ == '__main__':
    parser = argparse.ArgumentParser(description='''
    Setup PuDB (symlink config folder).
    ''', formatter_class=argparse.RawDescriptionHelpFormatter)
    args = parser.parse_args()
    setup()

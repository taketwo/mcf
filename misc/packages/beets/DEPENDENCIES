# ReplayGain plugin uses Gstreamer, which requires pygobject to be installed into
# the virtual environment. This, in turn, requires these other two ubuntu libs.
libgirepository-2.0-dev
gobject-introspection
gir1.2-gstreamer-1.0
# Some of the plugins needs pycairo, which in turn requires dev library.
libcairo2-dev
pipx: beets[fetchart] pygobject
